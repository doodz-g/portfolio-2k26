"use client";
import { useEffect,useRef } from "react";

export function MotionEffects(){const progressRef=useRef<HTMLDivElement>(null);useEffect(()=>{const reduced=window.matchMedia("(prefers-reduced-motion: reduce)").matches;const targets=document.querySelectorAll("main section > .container, .case-card, .experience-card, .skill-card");if(reduced){targets.forEach(target=>target.classList.add("is-visible"));return}targets.forEach(target=>target.classList.add("reveal-item"));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("is-visible");observer.unobserve(entry.target)}}),{threshold:.08,rootMargin:"0px 0px -50px"});targets.forEach(target=>observer.observe(target));let frame=0;const update=()=>{if(frame)return;frame=requestAnimationFrame(()=>{const distance=document.documentElement.scrollHeight-innerHeight;const progress=distance>0?scrollY/distance:0;if(progressRef.current)progressRef.current.style.transform=`scaleX(${progress})`;frame=0})};update();addEventListener("scroll",update,{passive:true});return()=>{observer.disconnect();removeEventListener("scroll",update);if(frame)cancelAnimationFrame(frame)}},[]);return <div ref={progressRef} className="scroll-progress" aria-hidden="true"/>}
