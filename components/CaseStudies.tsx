import Link from "next/link";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faArrowRight } from "@fortawesome/free-solid-svg-icons";
import { caseStudies } from "@/data/caseStudies";

export function CaseStudies(){return <div className="mt-12 grid gap-5 md:grid-cols-2">{caseStudies.map((study,index)=><article key={study.slug} className="case-card"><div className="flex items-start justify-between gap-4"><span className="display text-3xl font-extrabold text-[var(--orange)]">{String(index+1).padStart(2,"0")}</span><span className="text-xs font-extrabold uppercase tracking-wider text-[var(--muted)]">{study.applicationType}</span></div><h3 className="display mt-8 text-2xl font-extrabold sm:text-3xl">{study.title}</h3><p className="mt-4 leading-7 text-[var(--muted)]">{study.summary}</p><div className="mt-6 flex flex-wrap gap-2">{study.testingTypes.slice(0,4).map(type=><span key={type} className="pill !px-3 !py-1.5">{type}</span>)}</div><Link href={`/case-studies/${study.slug}`} className="focus-ring mt-8 inline-flex min-h-11 items-center gap-2 self-start font-extrabold">View case study <FontAwesomeIcon icon={faArrowRight} className="h-3"/></Link></article>)}</div>}
