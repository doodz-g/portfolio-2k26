import Image from "next/image";
import Link from "next/link";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faArrowRight,faArrowUpRightFromSquare } from "@fortawesome/free-solid-svg-icons";
import { projects } from "@/data/projects";
import { ProjectImagePlaceholder } from "@/components/ProjectImagePlaceholder";

export function FeaturedProjects(){return <div className="mt-12 grid gap-6 md:grid-cols-2">{projects.map(project=><article key={project.slug} className="project-card overflow-hidden rounded-[1.5rem] border border-[var(--line)] bg-white"><div className="relative aspect-[16/9]">{project.image?<Image src={project.image} alt={`${project.name} application preview`} fill sizes="(max-width:768px) 100vw,50vw" className="object-cover"/>:<ProjectImagePlaceholder projectName={project.name} projectType={project.type} compact/>}</div><div className="p-6 sm:p-8"><div className="flex items-center justify-between gap-4"><p className="text-xs font-extrabold uppercase tracking-[.14em] text-[var(--orange)]">Personal project</p><span className="pill !px-3 !py-1.5">{project.status}</span></div><h3 className="display mt-5 text-3xl font-extrabold">{project.name}</h3><p className="mt-2 text-xs font-extrabold uppercase tracking-wider text-[var(--muted)]">{project.category}</p><p className="mt-4 leading-7 text-[var(--muted)]">{project.summary}</p><div className="mt-6 flex flex-wrap gap-2">{project.stack.slice(0,4).map(tech=><span className="pill !px-3 !py-1.5" key={tech}>{tech}</span>)}</div><div className="mt-7 flex flex-wrap items-center gap-5"><Link href={`/projects/${project.slug}`} className="focus-ring inline-flex min-h-11 items-center gap-2 font-extrabold">View project <FontAwesomeIcon icon={faArrowRight} className="h-3"/></Link>{project.liveUrl&&<a href={project.liveUrl} target="_blank" rel="noreferrer" className="focus-ring inline-flex min-h-11 items-center gap-2 font-extrabold text-[var(--muted)]">Live demo <FontAwesomeIcon icon={faArrowUpRightFromSquare} className="h-3"/></a>}</div></div></article>)}</div>}
